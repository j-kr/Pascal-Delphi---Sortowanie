// PL
Program przedstawia implementacje alorytmów sortowania (bubble, selection, insertion, merge, courting, heap, bucket, quick)

//ENG
This program shows us implementation of different types of sorting (bubble, selection, insertion, merge, courting, heap, bucket, quick)